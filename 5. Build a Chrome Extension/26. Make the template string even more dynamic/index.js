// template strings/literals

const recipient = "James";

// Create a new variable, sender, and set its value to a name
const sender = "Roger";

// Use your sender variable instead of "Per"
const email = `Hey ${recipient}! How is it going? Cheers, ${sender}`;
console.log(email)
